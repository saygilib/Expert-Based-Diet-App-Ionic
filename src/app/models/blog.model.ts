export class Blog {
  title: string;
  link: string;
  text: string;
  constructor(title: string, link: string, text: string) {
    this.title = title;
    this.link = link;
    this.text = text;
  }
}
